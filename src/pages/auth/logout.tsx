import { Layout, LogOutModal } from "@components/layout"

export const Logout = () => {
    // Render
    return (
        <Layout.Dashboard>
            <LogOutModal />
        </Layout.Dashboard>
    )
}
